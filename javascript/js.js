// btn-1
document.getElementById("btn-1").addEventListener("click", function(event){
    event.preventDefault()
    Alert()

    // Task-btn a man komailam 
    const convertTaskNum = parseInt(document.getElementById("Task-num").innerText)
    const countTN = document.getElementById("Task-num").innerText= convertTaskNum - 1
    value(countTN)

    // Nav-num a mun barailam 
    const convertNavNum = parseInt(document.getElementById("nav-num").innerText)
    document.getElementById("nav-num").innerText= convertNavNum + 1

    // add comment 
    const commentContainer = document.getElementById
    ("comment-container")
    const newComment = document.createElement('p')
    newComment.style.padding='10px'
    newComment.style.paddingBottom='5px'

    // time 
    const now = new Date();
    const locleTime = now.toLocaleTimeString()

    newComment.innerHTML = `&#x1F60D You have completed the task <b>Fix Mobile Button Issue</b> at  ${ locleTime}`;
    commentContainer.appendChild(newComment)

    // btn disable
    document.getElementById("btn-1").disabled = true
    document.getElementById("btn-1").style= 'opacity: 0.2'
    document.getElementById("btn-1").style.cursor = "default"

})
// btn-2
document.getElementById("btn-2").addEventListener("click", function(event){
    event.preventDefault()
    Alert()

    // Task-btn a man komailam 
    const convertTaskNum = parseInt(document.getElementById("Task-num").innerText)
    const countTN = document.getElementById("Task-num").innerText= convertTaskNum - 1
    value(countTN)

    // Nav-num a mun barailam 
    const convertNavNum = parseInt(document.getElementById("nav-num").innerText)
    document.getElementById("nav-num").innerText= convertNavNum + 1

    // add comment 
    const commentContainer = document.getElementById
    ("comment-container")
    const newComment = document.createElement('p')
    newComment.style.padding='10px'
    newComment.style.paddingBottom='5px'

    // time 
    const now = new Date();
    const locleTime = now.toLocaleTimeString()

    newComment.innerHTML = `&#x1F496 You have completed the task <b>Add Dark Mode</b> <br> at ${ locleTime}`;
    commentContainer.appendChild(newComment)

    // btn disable
    document.getElementById("btn-2").disabled = true;
    document.getElementById("btn-2").style= 'opacity: 0.2';
    document.getElementById("btn-2").style.cursor = "default"
})
// btn-3
document.getElementById("btn-3").addEventListener("click", function(event){
    event.preventDefault()
    Alert()

    // Task-btn a man komailam 
    const convertTaskNum = parseInt(document.getElementById("Task-num").innerText)
    const countTN = document.getElementById("Task-num").innerText= convertTaskNum - 1
    value(countTN)

    // Nav-num a mun barailam 
    const convertNavNum = parseInt(document.getElementById("nav-num").innerText)
    document.getElementById("nav-num").innerText= convertNavNum + 1

    // add comment 
    const commentContainer = document.getElementById
    ("comment-container")
    const newComment = document.createElement('p')
    newComment.style.padding='10px'
    newComment.style.paddingBottom='5px'

    // time 
    const now = new Date();
    const locleTime = now.toLocaleTimeString()

    newComment.innerHTML = `&#x1F33A You have completed the task <b> Optimize  Home page </b> <br>at  ${ locleTime}`;
    commentContainer.appendChild(newComment)

    // btn disable
    document.getElementById("btn-3").disabled = true;
    document.getElementById("btn-3").style= 'opacity: 0.2';
    document.getElementById("btn-3").style.cursor = "default"

})
// btn-4
document.getElementById("btn-4").addEventListener("click", function(event){
    event.preventDefault()
    Alert()

    // Task-btn a man komailam 
    const convertTaskNum = parseInt(document.getElementById("Task-num").innerText)
    const countTN = document.getElementById("Task-num").innerText= convertTaskNum - 1
    value(countTN)

    // Nav-num a mun barailam 
    const convertNavNum = parseInt(document.getElementById("nav-num").innerText)
    document.getElementById("nav-num").innerText= convertNavNum + 1

    // add comment 
    const commentContainer = document.getElementById
    ("comment-container")
    const newComment = document.createElement('p')
    newComment.style.padding='10px'
    newComment.style.paddingBottom='5px'

    // time 
    const now = new Date();
    const locleTime = now.toLocaleTimeString()

    newComment.innerHTML = `&#x1F3C6 You have completed the task <b>Add new emoji 🤲</b> <br>at  ${ locleTime}`;
    commentContainer.appendChild(newComment)

    // btn disable
    document.getElementById("btn-4").disabled = true;
    document.getElementById("btn-4").style= 'opacity: 0.2';
    document.getElementById("btn-4").style.cursor = "default"
})
// btn-5
document.getElementById("btn-5").addEventListener("click", function(event){
    event.preventDefault()
    Alert()

    // Task-btn a man komailam 
    const convertTaskNum = parseInt(document.getElementById("Task-num").innerText)
    const countTN = document.getElementById("Task-num").innerText= convertTaskNum - 1
    value(countTN)

    // Nav-num a mun barailam 
    const convertNavNum = parseInt(document.getElementById("nav-num").innerText)
    document.getElementById("nav-num").innerText= convertNavNum + 1

    // add comment 
    const commentContainer = document.getElementById
    ("comment-container")
    const newComment = document.createElement('p')
    newComment.style.padding='10px'
    newComment.style.paddingBottom='5px'

    // time 
    const now = new Date();
    const locleTime = now.toLocaleTimeString()

    newComment.innerHTML = `&#x1F525 You have completed the task <b>Integrate OpenAI API</b> <br> at  ${ locleTime}`;
    commentContainer.appendChild(newComment)

    // btn disable
    document.getElementById("btn-5").disabled = true;
    document.getElementById("btn-5").style= 'opacity: 0.2';
    document.getElementById("btn-5").style.cursor = "default"
})
// btn-6
document.getElementById("btn-6").addEventListener("click", function(event){
    event.preventDefault()
    Alert()

    // Task-btn a man komailam 
    const convertTaskNum = parseInt(document.getElementById("Task-num").innerText)
    const countTN = document.getElementById("Task-num").innerText= convertTaskNum - 1
    value(countTN)

    // Nav-num a mun barailam 
    const convertNavNum = parseInt(document.getElementById("nav-num").innerText)
    document.getElementById("nav-num").innerText= convertNavNum + 1

    // add comment 
    const commentContainer = document.getElementById
    ("comment-container")
    const newComment = document.createElement('p')
    newComment.style.padding='10px'
    newComment.style.paddingBottom='5px'

    // time 
    const now = new Date();
    const locleTime = now.toLocaleTimeString()

    newComment.innerHTML = `&#x1F339 You have completed the task <b>Improve Job searching</b> <br> at ${ locleTime}`;
    commentContainer.appendChild(newComment)

    // btn disable
    document.getElementById("btn-6").disabled = true;
    document.getElementById("btn-6").style= 'opacity: 0.2';
    document.getElementById("btn-6").style.cursor = "default"

    
})





// new tab 
document.getElementById("new-today-btn").addEventListener("click", function (){
    
    window.open("index-2.html");
})

// random color 
document.getElementById("random-color-btn").addEventListener("click", function () {
    function randomC() {
        let l = "0123456789ABCDEF0123456789ABCDEF";
        let c = "#";
        for (let i = 0; i < 6; i++) {
            c += l[Math.floor(Math.random() * 32)];
        }
        return c;
    }

    document.body.style.backgroundColor = randomC();
});

// date 
const today = new Date();
const options = { weekday: 'short', month: 'short', day: '2-digit', year: 'numeric' };
const formattedDate = today.toLocaleDateString('en-US', options).replace(',', '');
document.getElementById("date").innerHTML=`<small>${formattedDate}</small>` 

